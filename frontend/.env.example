# Base URL of the backend API. In local dev this is proxied by Vite so you can
# leave it unset (falls back to "/api"). In production (Render), set this to
# your deployed backend's URL, e.g. https://your-backend.onrender.com/api
VITE_API_BASE_URL=
